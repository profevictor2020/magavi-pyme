services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-magavi}
      POSTGRES_USER: ${POSTGRES_USER:-magavi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-magavi_dev_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-magavi}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    env_file: .env
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      LLM_OLLAMA_BASE_URL: http://llm-inference:11434
      CELERY_BROKER_URL: redis://redis:6379/0
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health/', timeout=2)"]
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 10s

  # Procesa OCR + estructuración de documentos en background (ver
  # docs/DECISIONS.md ADR-007, docs/ROADMAP.md Fase 9). Comparte imagen
  # y código con `backend` vía el mismo bind mount, así que ve los
  # archivos que `backend` sube a MEDIA_ROOT sin configuración extra.
  # No corre `migrate` por su cuenta (SKIP_MIGRATIONS): si lo hiciera,
  # correría en paralelo con el `migrate` de `backend` contra la misma
  # base de datos recién creada y ambos chocarían (UniqueViolation al
  # crear las mismas tablas dos veces). En cambio espera a que
  # `backend` esté healthy (lo que solo pasa después de que su propio
  # `migrate` terminó y el servidor ya responde).
  worker:
    build: ./backend
    env_file: .env
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      CELERY_BROKER_URL: redis://redis:6379/0
      SKIP_MIGRATIONS: "true"
    volumes:
      - ./backend:/app
    command: celery -A config worker --loglevel=info
    depends_on:
      backend:
        condition: service_healthy

  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend

  # Opcional, apagado por defecto (no lo levanta `docker compose up` ni
  # el job de CI): motor de inferencia self-hosted para el asistente
  # (ver docs/DECISIONS.md ADR-004, docs/ROADMAP.md Fase 8). Requiere
  # cómputo real (GPU o CPU capaz) y varios GB de disco para el modelo,
  # que no están disponibles en todos los entornos de desarrollo.
  #
  # Uso: docker compose --profile llm up
  #      docker compose exec llm-inference ollama pull qwen2.5:7b-instruct
  llm-inference:
    image: ollama/ollama:latest
    profiles: ["llm"]
    volumes:
      - ollama_data:/root/.ollama
    ports:
      - "11434:11434"

volumes:
  postgres_data:
  frontend_node_modules:
  ollama_data:
